<resources>


    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>


        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="titleTextColor">@color/colorWhite</item>


    </style>


    <style name="DetailActivityTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>


    <style name="layoutWrapWidthHeight">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="ViewStyle1" parent="layoutWrapWidthHeight">
        <item name="android:layout_gravity">end</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:paddingRight">3dp</item>
    </style>

    <style name="ViewStyle2" parent="layoutWrapWidthHeight">
        <item name="android:layout_gravity">bottom|end</item>
        <item name="android:paddingRight">3dp</item>
    </style>

    <style name="SynopsisStyle" parent="layoutWrapWidthHeight">
        <item name="android:layout_gravity">bottom|left</item>
    </style>

    <style name="MainProgressBarStyle" parent="layoutWrapWidthHeight">
        <item name="android:layout_centerHorizontal">true</item>
        <item name="android:layout_centerVertical">true</item>
        <item name="android:visibility">gone</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
